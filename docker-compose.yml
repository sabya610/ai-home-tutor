services:
  tutor:
    build: .
    image: sabya610/ai-home-tutor:v3
    container_name: ai-home-tutor
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/backend/data
    restart: unless-stopped
